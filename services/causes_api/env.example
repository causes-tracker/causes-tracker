DATABASE_URL=postgresql://causes:causes@localhost:5432/causes

# Required for first-time bootstrap only; can be removed after admin is created.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Optional: export traces to Honeycomb.
# HONEYCOMB_API_KEY=
# HONEYCOMB_ENDPOINT=https://api.honeycomb.io:443

# Optional: override gRPC listen address (default: [::]:50051).
# BIND_ADDR=[::]:50051
