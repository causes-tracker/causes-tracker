load("//build:rust.bzl", "rust_library", "rust_test")
load("//tools/lint:format_check.bzl", "format_srcs")

rust_library(
    name = "db_aws",
    srcs = glob(["src/**/*.rs"]),
    visibility = ["//visibility:public"],
    deps = [
        "//lib/rust/db_pool",
        "@crates//:anyhow",
        "@crates//:aws-config",
        "@crates//:aws-sdk-rds",
        "@crates//:aws-smithy-http-client",
        "@crates//:aws-types",
        "@crates//:sqlx",
        "@crates//:tracing",
    ],
)

rust_test(
    name = "db_aws_test",
    crate = ":db_aws",
    deps = [
        "@crates//:aws-credential-types",
        "@crates//:aws-smithy-async",
        "@crates//:tokio",
    ],
)

format_srcs()
